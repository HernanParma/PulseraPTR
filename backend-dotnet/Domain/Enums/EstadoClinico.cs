namespace Domain.Enums;

/// <summary>
/// Estado clínico derivado de reglas de negocio (FC, SOS, etc.).
/// </summary>
public enum EstadoClinico
{
    NORMAL = 0,
    ADVERTENCIA = 1,
    CRITICO = 2
}
